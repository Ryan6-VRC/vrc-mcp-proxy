"""vrc-mcp-proxy — an owned stdio MCP interception proxy for MCP-for-Unity.

See docs/design.md for why this exists (the transport lies) and docs/bump-runbook.md
for how to move the upstream pin. The pin lives in one place: config.UPSTREAM_PACKAGE.
"""

__version__ = "0.1.0"
