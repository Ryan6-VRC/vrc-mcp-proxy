# vrc-mcp-proxy — design

One owned stdio MCP proxy between the MCP client and MCP-for-Unity (pinned `mcpforunityserver==10.1.0`; upstream is not ours to patch). Server key stays `UnityMCP` in `.mcp.json`, so every `mcp__UnityMCP__*` name, doc reference, and settings matcher survives unchanged. **A behavior ships only if a named standing doc line retires with it** — the ledger below is the deliverable's measure.

## Architecture

Python/uv. The proxy spawns the pinned upstream server as a stdio subprocess and relays JSON-RPC both ways. Three layers, in order:

1. **Startup canary.** Validate upstream's tool names + schemas against the committed baseline (`canary-baseline-10.1.0.json`, 47 tools). Drift → refuse to serve the drifted tool; emit one loud error naming the bump runbook. The upstream list is **staged** — `tools/list_changed` fires when an Editor connects and custom tools register — so the canary validates the baseline subset it knows (names it shapes/allows must exist with matching schemas) rather than demanding list equality at one instant. Behaviors that key on response *strings* (console-strip) are schema-invisible — the runbook, not a runtime canary, re-validates those.
2. **Allowlist.** Tools absent from the allowlist are stripped from `tools/list` and refused on call. Never expose raw and shaped variants side by side. Resources pass through untouched.
3. **Per-tool transforms** (requests and responses), each independently disableable.

### Two standing rules for a transform

**No string keying on upstream prose, for anything that rewrites.** A transform that changes a verdict keys on structure — a `success` field, a payload shape — never on upstream's wording, because wording drifts between versions with no canary watching (`canary.py` baselines `inputSchema` only) and a key that silently stops matching turns a correction off in silence. Two carve-outs, both argued at their own rows: the venue guard keys on a marker **the proxy itself emitted** two hops earlier, not upstream prose; the compile notes key on compiler strings but only ever *append*, which is the standing `manage_gameobject_inactive_note` holds — a stale key costs the note and nothing else.

**A transform is for what the proxy can repair; a refusal is for what never reaches it.** Where upstream has already destroyed the evidence — truncated it, or discarded a return value and substituted its own verdict — no downstream transform can reconstruct it, and one that appears to have is worse than the silence. Refuse and redirect to the door that still has the truth. `read_console` (whole tool) and F22's `move`/`rename` (per-action) are the two instances.

## Failure-family verdicts → behaviors

| Failure | v10.1.0 verdict (source diff + live repro) | Behavior |
|---|---|---|
| Unpinned silent crosstalk | Upstream's own hard-error guard is **live-confirmed** (2+ editors and no pin → error; 1 → auto-select) but its liveness gate is a 0.3s socket probe: a busy/compiling editor can drop out of the count, undercounting to 1 and silently auto-selecting the wrong venue (G50). | **`instance_guard`** (request guard, probe-free heartbeat count from `instances.live_instances`): unpinned call + ≥2 live editors within the freshness window (`GUARD_WINDOW_S`, 180s) → refuse, naming the instances and `set_active_instance`; exempts `set_active_instance` itself. Closes the short-block undercount; a block *longer* than the window still ages the heartbeat out (residual, closed by the `proxy_project_root` pin-time backstop, G50-B — its own independently-disableable behavior, decoupled from `instance_guard`, F7). |
| F48 retry misroute | **Not reproduced — no recorded live incident, and no live repro attempted** (it needs two Editors; only one was free). Source-read from the pinned upstream: `unity_connection.py:443–466` re-discovers the port on retry, and when the pinned instance has dropped out of discovery, `stdio_port_registry.get_port` (`:53–57`) falls through to `PortDiscovery.discover_unity_port()` — an instance-agnostic scan defaulting to 6400. So the misroute needs **two** conditions, a failed attempt *and* the pin vanishing from discovery, and can only bite with 2+ editors live. `atelier#51` shipped under this tag but is a doc-only fix for the adjacent port-stability finding; this row is the unshipped guard #51 was checking for. **Justified by census, not by incident** (as `execute_code_safety_off` was): across 4,390 `execute_code` calls in 178 sessions, 119 carried a hand-written venue guard — all from 4 sessions, every one multi-venue. Where two editors are live agents *do* write a guard unprompted; what they write is `Application.dataPath.Contains("<name typed from memory>")`, a substring match that passes for `AvatarProject/…/Sandbox` as readily as for `Sandbox`. | **`execute_code_venue_guard`** (request prelude + response rewrite). Prepends an exact `Application.dataPath` comparison against the pinned editor's own heartbeat `project_path` — the same string from the same process, so this is two reads of one value, not two path spellings reconciled. Sits **before** the idempotency `SessionState` write, so a misroute leaves no key and mutates nothing. Never guesses a venue; and since the doc line telling agents to hand-write their own check retires with this behavior, an unguarded forward would be a call nobody checks — so a pin that is *set* yet resolves to none or several is **refused**, while an unpinned call (`instance_guard`'s business) and an empty heartbeat directory still forward. The pin is stored canonically as `Name@hash` at commit time: left raw, a bare-port pin would put every later resolve on the freshness-filtered arm for the whole session, so a block longer than `GUARD_WINDOW_S` would age the heartbeat out and drop the guard *inside the long-block window the misroute needs*, with `instance_guard` unable to compensate (the pin is truthy). The response rewrite is additionally bound to a call the proxy actually guarded, so marker-leading text returned by an unguarded snippet is not turned into a fabricated error. Its resolver `instances.resolve_assets_path` filters on liveness for port/no-selector lookups and deliberately does not for `Name@hash` (hash = `SHA1(dataPath)[:8]`, so hash→path is total and stale-tolerant; filtering there would drop the guard exactly when a long block ages the heartbeat out). Refusal text scopes the claim to *this* editor — "nothing ran HERE" — because the failed first delivery often did run in the pinned one, and an unqualified "nothing ran" would invite a re-run that lands the mutation twice. **Residual, stated (second one):** the guard's 5 emitted lines shift every compile-error line number upstream reports — disclosed by `execute_code_prelude_offset_note`, whose row explains why the numbers are disclosed rather than corrected. **Residual, stated:** `execute_code` is the only tool that can carry a probe; every other tool stays exposed to the same fallback, mitigated only by `instance_guard`, `proxy_project_root`, and the full-`Name@hash` rule. **When this row shipped, `proxy_project_root` was reaching no client at all** (§Two surfaces), so the residual was larger than the row claimed until that was fixed; `instance_guard` was unaffected, because a refusal replaces the whole result rather than editing it. The response half is the proxy's **first content-keyed response transform** — permitted only because the key is a marker *we* emitted two hops earlier, not upstream prose (what §Two standing rules' no-string-keying rule forbids); anchored to the start of `data.result` and scoped to `action:"execute"`, because the bridge's `get_history` echoes a 500-char preview of the snippet source, which contains the marker as a literal. |
| G22 double-execute | Retry lives in upstream `send_command`'s connection loop (re-send on recv exception, 30s recv / 90s deadline, no request ids) — **below the MCP layer; a proxy-side journal can never see the duplicate.** **Live-reproduced on 10.1.0 twice in one run** (35s snippet → 2 on-disk marker writes; a "timed-out" setup snippet re-created an already-moved asset). | **Snippet idempotency guard** (execute_code request transform): proxy prepends a minted-GUID `SessionState` check-and-set; a re-delivered snippet returns the cached result if the first run finished, else a `duplicate-suppressed` marker. Converts "completed / completed-twice / failed" into "executed once; verify on disk". **Residual:** the wrap's 6-line preamble shifts reported compile-error line numbers, and its 4-line trailer is where a snippet with unbalanced braces reports — both disclosed by `execute_code_prelude_offset_note`. |
| F22 move lies | **First-order lie, and the failure verdict carries no information**: `MoveAsset call failed unexpectedly` on moves that landed, **32/32 through the proxy against an idle editor** — 24 freshly-created assets and 8 more after a forced domain reload, so this is not import settling. GUID preserved and `LoadAssetAtPath` non-null on every one, so the move is real and not merely a file appearing on disk. The error strings vary **by cause, not randomly**: `failed unexpectedly` on the lie, `Source asset not found` when the source genuinely is absent, `An asset already exists at the destination path` on a real collision. Second lie in the same arm: a **bare `destination` resolves to `Assets/<name>`**, not beside the source (measured with and without an extension; falsified against the sibling model by pre-creating the root path, which upstream then reports as a collision at `Assets/<name>`) — so a rename relocates the asset to the project root and reports failure for it. | **Refusal, not correction — `manage_asset_mutation_guard`** (request side, `move`/`rename`). A truth-correction here reconstructs by disk-stat the exact fact upstream was handed and discarded, and cannot reach the case that matters: a move whose source never existed onto a destination that already did leaves disk state identical to a real move. `AssetDatabase.MoveAsset` separates them outright (empty string = landed, else the error), and it *rejects* a bare name rather than relocating for it, so the refusal routes there — the `read_console` rule, applied to an arm rather than a tool. **`delete` forwards untouched**: upstream reports it honestly (landed → `success:true`; never-existed → upstream's own `Asset not found`), so the correction that used to run there fired only on correctly-reported failures and rewrote them to success, making a mistyped path read as a completed delete. Three things the removal costs or leaves open, stated rather than discovered later. **The delete arm's orphan-meta detector went with it** — the branch that flagged "asset gone, `.meta` still present" as an unclean delete never rewrote anything and nothing replaces it. **Only `delete` was measured** among the mutating actions that still forward; `duplicate` (`CopyAsset`) and `create_folder` return values the same handler shape could discard, and are merely not-yet-suspect. **The two refusals compose**: the redirect routes into `execute_code`, which this proxy itself refuses when a set pin resolves to none or several live editors, so during a block long enough to age the heartbeat out there is no route to a move at all — deliberate (refusing a mutation whose venue is unresolvable is the house rule), and named in the refusal text. Verified against the bridge for this pin: `AssetDatabase.MoveAsset` is **not** in `ExecuteCode.cs`'s `_blockedPatterns` (`DeleteAsset` and `MoveAssetToTrash` are), so the redirect works with `safety_checks` left on. |
| F23 stale search | **Not reproduced** — `get_info` on the old path is immediately correct. Real quirk found instead: `search` scoped to a nonexistent path silently returns **global** results instead of empty/error. | **None.** One doc line on the scope fallback. |
| G21 heavy-work timeout | Bounded (90s deadline) but unchanged in kind; stdio timeouts are hardcoded upstream, not env-tunable. | **Appended note** on both timeout strings: "error ≠ didn't-run; verify on disk". Heavy imports additionally route through an owned `[AgentTool]` door (vrc-unity-tools) that owns its result contract. |
| F37 `using` cascade | Wrapper byte-identical; method-body constraint is structural. True hoist is impossible from outside the wrapper: C# forbids using-directives in a method body, and lexical re-qualification of identifiers is semantic work a regex can't do safely. | **Corrective refusal** (request guard): top-level `using` detected → immediate error naming the fix and the six pre-imported namespaces. The error replaces the standing doc line. |
| F9 console mis-tag, F44 `types`/`filter_text` no-ops, F12 first-line truncation | `read_console` mis-tags benign lines as errors (`InferTypeFromMessage` substring-matches "Exception"), ignores its own `types`/`filter_text` params, and — decisively — returns only the **first line** of every entry: the Unity-side handler reads the full message, splits on newline, and keeps `[0]`. A warning whose payload is a list arrives as its header with the list gone. | **Denied, not transformed** (`_REDIRECTS` in `allowlist.py`). The truncation happens in the C# before the response is built, so the body never reaches this process and a transform here can only reshape what upstream already sent. Partial recovery through `include_stacktrace` is worse than none: a payload line is salvaged when it matches upstream's stack-frame heuristic and dropped when it doesn't, with nothing in the response telling the caller which happened. The refusal routes to `ReportConsole` (`com.ryan6vrc.agent-tools`), which reads `LogEntries` directly and owns the type-from-mode-bits classification and the benign-noise predicates this transform used to carry. |
| G28 empty scene | `NewSceneSetup.EmptyScene` hardcoded; server instruction is advisory fiction. | **None.** One-line doc caveat. |
| F52 Roslyn background-compile hang | Editor stays responsive but `execute_code` under the default (Roslyn) compiler can enter a per-editor state where the compile never returns — upstream sends nothing, so it rides to the client's 1800s idle cap. **Ground truth:** run-5 T6, two consecutive calls hung ~1800s each; codedom ran the identical snippet in 2.8s. Distinct from the main-thread-block mode (`.Result`/modal), which upstream bounces at ~36s. | **`execute_code_watchdog`** (request-armed timer): on an `action:"execute"` call, a per-call timer (default 120s, `VRC_MCP_PROXY_EXECUTE_TIMEOUT_S`, tolerant parse) synthesizes a labeled timeout on expiry that routes to `compiler:"codedom"` (→ editor restart if C#7+/mutating), then drops the late real response. Retires **no** doc line — it converts a ~30-min silent hang into a bounded, labeled timeout naming the recovery; it does **not** replace `timeout_notes` (still the ~36s main-thread-block note). |
| `execute_code` safety_checks | The bridge's `_blockedPatterns` (ExecuteCode.cs) is a case-insensitive **substring** match over snippet text. It stops `AssetDatabase.DeleteAsset` while passing `Object.DestroyImmediate(asset, true)`, an overwriting `File.WriteAllText`, and a `CopyAsset` over a live prefab — it does not cover what actually destroys work in this workspace. It cannot see paths, so cleanup under `Assets/Agent/Scratch/` and a vendor-tree wipe are indistinguishable to it, and its refusal advertises the bypass while asking about *intent* ("if this is intentional") when the failure mode is *scope*. Store census: 140 blocks (109 `DeleteAsset`) against `safety_checks:false` appearing 412 times across 91 sessions — already passed pre-emptively, taught by the doc line this retires. What is given up alongside the delete patterns: the four literal infinite-loop forms (`while(true)`, `while (true)`, `for(;;)`, `for (;;)`), the one class where a substring match is defensible, since such a loop wedges the Editor main thread and `execute_code_watchdog` bounds only this proxy's view of that hang. It never covered the likelier `while (x)`. | **`execute_code_safety_off`** (request transform): send `safety_checks:false` when the caller left it unset; an explicit value either way is left alone. Deliberately **no** replacement guard for `Process.Start`/`Process.Kill`/`EditorApplication.Exit` — nothing reaches those by accident, a snippet that wants them has reflection and a dozen other doors, and every `Exit` block measured was an intentional shutdown. Path-scoping (the original ask) is not reachable here: `safety_checks` is one per-call boolean, not a filter, and inferring "these paths are scratch" from C# text fails open on exactly the computed-path bulk delete that matters most. |
| `manage_scene` per-action argument silence | `manage_scene` takes one flat argument set for every action (twelve in the pinned enum, fourteen cases in the C# switch); the bridge binds every argument for every action (`ToSceneCommand`) and each handler reads only its own. An argument the action ignores is accepted, forwarded, and dropped without an error — measured: `get_hierarchy` with `target` returns the full root list with `"scope":"roots"`, which reads as the target's children. | **`manage_scene_arg_guard`** (request guard): refuse on a **curated** table of confusable pairs, each verified against its handler, naming the argument that scopes — but only when the caller supplied the wrong argument *alone*. Supplying the right one too means the call already works, and refusing it would both break a working call and, where the intended argument was already sent, echo back a "re-issue with" instruction identical to the request just made: a retry loop with no exit. Curated, not exhaustive: a complete per-action consumption map is vendor C# no canary watches — schemas are pinned, handler bodies are not — so a misreading would turn a working call into a hard refusal. A missing pair costs only the silence that exists today. |
| `manage_camera` screenshot litter | Folder resolution is caller `output_folder` → user EditorPrefs → built-in `Assets/Screenshots` (`ScreenshotPreferences.Resolve`), and nothing in the tool description says so. Unaugmented shots land in the venue's Assets root as PNGs plus `.meta` files (measured: two venues littered from one batch, undisclosed by the worker). The parameter exists — it is simply undefaulted. | **`manage_camera_screenshot_output`** (request transform): default an unset `output_folder` to `Assets/Agent/Scratch/Screenshots`, treating blank as unset (upstream resolves with `IsNullOrWhiteSpace`). An explicit folder always wins; what this does displace is the per-user EditorPrefs tier, which becomes unreachable through the proxy — acceptable because no committed file records it and the vendor's own Tools window calls it per-call-overridable, but it is a narrowing, not none. `action` carries no enum here and the bridge lowercases it, so the match normalizes case rather than trusting the raw string. |
| `execute_code` compile traps | Two `docs/unity.md` §Sharp edges bullets described compile failures whose text is deterministic: bare `Object.DestroyImmediate` (`'Object' is an ambiguous reference between 'UnityEngine.Object' and 'object'`) and aliasing a static class (`'AssetDatabase' is a type, which is not valid in the given context`). Both arrive as ordinary success-shaped results carrying `success:false` + `data.errors`, so both are annotatable — unlike their `unity.md` neighbours (`Type.GetType` null, `GameObject.Find` inactive), which return null on a *successful* call and so lift nowhere. **Live-measured** (Sandbox, 2026-08-12), and the measurement changed the design twice. First: **Roslyn and CodeDom spell both diagnostics incompatibly** — CodeDom reverses the ambiguity's operands, quotes with `` ` ``…`'`, and rewords the alias case entirely (`` `UnityEditor.AssetDatabase' is a `type' but a `variable' was expected ``). CodeDom is a live door — 129 explicit `compiler:"codedom"` calls across 8 sessions, against 4,411 `action:"execute"` calls in 175 sessions (session-store census, 2026-08-12; denominator counts *execute* actions only, so it is **not** comparable to the F48 row's 4,390-across-178, which counts `execute_code` calls of every action) — so a single-dialect key would be dead there while the prose that covered both was already deleted. Second: the trap class is **wider than the prose ever said** — `Random.Range` yields the same ambiguity (`'UnityEngine.Random' and 'System.Random'`), which no doc line ever named. | **`execute_code_compile_notes`** (response transform): on a compile failure, append one note per distinct trap — never per matching line, since one mistake yields two errors on both compilers. The ambiguity note names the collision and the fully-qualified fix; the type-in-value-position note states **the condition, not the cause**, because both keys fire on *any* type used where a value belongs and asserting "you aliased a static class" would infer a purpose from a state (§Lifting's second condition). **The keying exception is argued at note standing only.** §Two standing rules' no-string-keying rule and the venue guard's "first content-keyed response transform" carve-out both govern transforms that *rewrite*; this one only appends, which is the standing `manage_gameobject_inactive_note` already holds ("a stale key only costs the note; the note never rewrites a verdict"). The gate itself is **structural** — `success:false` plus a `data.errors` entry in the compiler's `Line <n>:` shape — so the only prose keys are the two trap fragments. Scoped to `execute` **and `replay`**, not `execute` alone: a compile failure is recorded in history (live-confirmed), and it is the class that genuinely recompiles on replay — a stored entry that once *succeeded* short-circuits on the idempotency guard's baked-in `SessionState` key and never reaches the compiler, while a failed one never wrote that key. Gating on `execute` would have left the trap live on the one door it most reliably re-fires through. The CodeDom key keeps `is a \`type'` rather than stopping at `` but a `variable' was expected ``: mcs templates that whole CS0118 family off one string (`` `namespace' ``, `` `method group' ``, `` `property' ``), so the shorter key would annotate `var x = System;` with a note whose opening claim — that a *type* was used — is false. Stated rather than papered over: these are *compiler* strings on Unity/Roslyn/mcs cadence, which no canary and no bump-runbook step can watch, so their staleness is **unmonitored — and costless**, because a missed match loses a note and nothing else. |
| Compile-error line numbers (**self-inflicted** — a residual of this proxy's own guards, not an upstream family) | The request-side prelude shifts every compile-error line number the proxy relays: `execute_code_venue_guard` emits 5 lines and `execute_code_idempotency_guard`'s preamble 6, so a caller's line 5 is reported as line 16 (offset 11, measured across three probes on both compilers). | **`execute_code_prelude_offset_note`** (response transform): disclose the offset, never apply it. Rewriting the numbers was designed and **rejected**. It is a payload rewrite keyed on third-party compiler prose — exactly the standing the note exception above does *not* extend to — and a stale key there emits a wrong line number *as fact*, which reads identically to a right one. It also needs bounds the response side cannot compute: `wrap_idempotent` emits 4 lines **after** the caller's code, so a snippet with one unbalanced brace reports in that trailer and would renumber onto lines the caller never wrote, while `_remember` stores the *post-transform* arguments and so cannot recover the caller's own line count. The note instead states the offset, both out-of-band bands, and that unbalanced braces/parens/comments are what puts an error there — the reader knows their own line count, which is the fact the proxy lacks. Silent at offset 0, which includes every `replay`: the stored snippet carries the *original* call's prelude, and this process no longer knows how big it was — so the trap notes ride a replay (see the row above) while the offset note deliberately does not. **Residual, stated:** a replayed compile failure therefore still reports shifted line numbers with nothing disclosing the shift. The note's wording is conditional for the same reason its number is measured — it names "its request-side guards" rather than both by name (at 6 the venue guard emitted nothing, at 5 the idempotency guard did), and the trailer clause appears only when the idempotency wrapper is actually on, since without it there is no trailer past the caller's last line to warn about. The count is **measured off the emitted strings** (`prelude_line_count`), never derived from cfg: the venue guard can be *enabled* while emitting nothing (unresolved venue, or an empty heartbeat directory, which forwards by design), making the true prelude 6 with the behavior reading "on" — the state every unit test sits in, so a cfg-derived count would be green throughout and wrong against a real Editor. `test_prelude_line_count_matches_what_transform_request_actually_injects` pins the count to what the generators actually emit, across all four configurations. |
| Inactive-target lookup | The bridge's `manage_gameobject` target search is active-only for every action but `modify` + `set_active:true` — **source-confirmed**, and `by_id` is filtered the same way (the id is matched against the active-only pool, never resolved directly), so re-locating with `find_gameobjects include_inactive:true` and handing the instanceId back fails identically. Not correctable from here: the tool's schema has no `include_inactive` and its `additionalProperties:false` forbids injecting one. | **`manage_gameobject_inactive_note`** (response transform): on a lookup-miss response for a target-taking action, append the note naming the one opting-in action, the instanceId trap, and the two routes that work (activate first, or `execute_code` with `transform.Find`). Diagnostic only — it never rewrites a verdict. |

Rejected: MCP-layer retry-dedup journal (the MCP client never re-sends `tools/call`; the model's own deliberate retries must not be suppressed, and upstream's re-send is invisible at this layer). Rejected: timeout config as a G21/G22 fix (no stdio knobs upstream).

## Two surfaces

A tools/call result carries the payload twice: `content` (text) and `structuredContent`. 46 of the 47 baseline tools declare an `outputSchema` — `manage_camera` is the only one that doesn't — and FastMCP emits `structuredContent` for every one of them. **That is the surface an MCP client shows the model.** So a response transform writes both, via `envelope.write_payload` / `envelope.add_note`; a direct assignment into `content` is the bug below, not a shortcut.

Every response transform wrote `content` alone until this was found, so `proxy_project_root`, the `manage_asset` truth-correction (since retired for a refusal — F22's row), the `manage_gameobject` inactive note, and `timeout_notes` (on the tools that swallow a timeout — the ones that let it raise return `isError` with no `structuredContent`, and their note always arrived) reached **no caller at all** for the life of the proxy. Measured live: on a real pin, `content` carried `proxy_project_root` and the client received a payload without it; on a failed `manage_asset` delete, `content` said `success:true` with the note while `structuredContent` said `success:false` — the two halves of one response contradicting each other, with the client reading the failing one. The `tool_error_result` behaviors (allowlist, canary, `instance_guard`, F37, the venue-guard rewrite, the F52 watchdog) were never affected: they replace the whole result, which then carries no `structuredContent`, and a client skips its schema check on `isError`.

The tests passed throughout because six ad-hoc fixtures each built a result with a `content` key only — a shape upstream never sends. They now route through one builder (`tests/helpers.py`), which is the actual repair; asserting on `content` alone reproduces the original blindness exactly.

`write_payload` writes **both surfaces or neither**, and only into a shape it can prove against the pre-mutation payload: a mirror (`structuredContent` equals it) or FastMCP's wrapper (`{"result": …}`, emitted for a tool whose outputSchema carries `x-fastmcp-wrap-result` — allowlisted `refresh_unity` is one). Anything else leaves the response exactly as upstream sent it and says so on stderr, naming the behavior that declined. The all-or-nothing arm is the load-bearing part: writing `content` alone on an unprovable shape would leave a verdict rewrite reading `success:true` on one surface and `success:false` on the other, which is this section's own bug with the surfaces swapped — an un-applied correction is the status quo, a half-applied one is a lie. `add_note` is the opposite case and writes `content` regardless, because a note reaching one surface is less visible, never contradictory. Proving beats modelling upstream's serializer, since a bump can reshape it with no canary watching (`canary.py` baselines `inputSchema` only), and equality is compared over canonical JSON because Python reads `{"success": 0}` and `{"success": false}` as equal. The bump runbook's repro driver flags any call whose surfaces disagree, and any call to an outputSchema-declaring tool that returns no `structuredContent` at all — the shape a client hard-errors on. The notes' *detection* stays keyed on `content`, which upstream writes from the same value.

`add_note` needs no such proof — an added key is safe beside a wrapper — but it writes `proxy_transport_note` rather than a name a payload-writing transform would also reach for, so one response can carry both a rewrite and a note without either clobbering the other.

**Watchdog id-uniqueness boundary** (documented, not fixed): the F52 watchdog's exactly-once + late-drop guarantee is keyed purely on the client's JSON-RPC request id, so it assumes ids stay unique while in flight — true for every compliant MCP client, Claude Code included. A client that reuses an id already armed/fired collapses the two calls into one id-space: the reused id's own bookkeeping overwrites the original call's, so that call's late real response (if it ever arrives) is no longer recognizable as stale and passes through instead of being dropped. Not evidenced to matter in practice — F52's own retry path (resending the suggested `codedom` snippet) is a new `tools/call` with a new id, never a reused one. Pinned by `test_execute_watchdog_id_reuse_after_fire_late_response_not_dropped`.

## Allowlist (transcript census, strict-call counts)

Expose: `execute_code`, `refresh_unity`, `set_active_instance`, `manage_scene`, `manage_editor`, `manage_asset`, `manage_packages`, `unity_reflect`, `find_gameobjects`, `execute_menu_item`, `manage_camera`, `manage_gameobject`, `debug_request_context`. Hide: `run_tests` + `get_test_job` (venue-denied — retires the tracked PreToolUse deny hook), `read_console` (truncation-denied, see the ledger row above), `manage_tools`, `manage_animation` (no evidenced use), all five asset-gen tools. A hidden tool later needed is a one-line allowlist edit.

A hidden tool with a right way to do the same thing gets a `_REDIRECTS` entry rather than the generic refusal: the refusal is the only moment the caller is guaranteed to be listening, so it spends that moment naming the door to use instead.

## Deprecation ledger (settled at PR time)

| Retires | Owner behavior |
|---|---|
| CLAUDE.md `execute_code` method-body standing line | F37 corrective refusal |
| unity.md §Sharp edges: F37 block, MACS/DestroyBlendTree noise, F9 substring note | F37 refusal + console-strip |
| — (nothing; the FBX importer "inconsistent result" strip retires no standing doc line — that noise came from an assay kickoff, not a doc. Kept to spare re-triage of known importer noise.) | console-strip (FBX predicate) |
| unity.md §MCP unpinned-routing bullet + CLAUDE.md session-start pin reminder wording + instance-pinning memory | `instance_guard` (+ `proxy_project_root`) |
| `.claude/settings.json` run_tests PreToolUse deny hook + bootstrap.md §run_tests-is-blocked | allowlist hide |
| unity.md §Sharp edges: the `safety_checks` blocked-pattern bullet and its `safety_checks=false` workaround | `execute_code_safety_off` (atelier PR lands with this one — the ratchet in `docs/tool-design.md` §Lifting) |
| unity.md §MCP: the "a mutating `execute_code` script opens with an `Application.dataPath` guard" clause | `execute_code_venue_guard` (atelier PR lands with this one — same ratchet). Retiring the clause is what makes this a *replacement* rather than a second guard beside the hand-written one: left standing, the doc keeps teaching the substring form the census shows agents actually write. |
| unity.md §Sharp edges: the bare-`Object.DestroyImmediate` bullet and the static-class-alias bullet, deleted whole (atelier PR lands with this one — the ratchet in `docs/tool-design.md` §Lifting). Both are **pure-reaction** traps — one wasted round trip, nothing mutated, nothing lost — which §Lifting says earn a check and no doc line, so nothing is left behind pointing here. The adjacent `GameObject.Find` bullet keeps its trap but is **rescoped** in the same PR: its clause "`execute_code` has no failure response to annotate" was written as a claim about the tool, and this behavior falsifies it. | `execute_code_compile_notes` |
| — (nothing; `execute_code_prelude_offset_note` retires no standing line — the offset was never written down, because it is this proxy's own residual and predates nobody's notice. A pure-reaction trap, and one the check now discloses at fire time.) | `execute_code_prelude_offset_note` |
| — (nothing; `manage_scene_arg_guard` and `manage_camera_screenshot_output` retire no standing line. Neither trap was ever written down: the first was read as a real answer about a target's children, the second was invisible until the litter was counted. Both are pure-reaction traps, which per `tool-design.md` §Lifting earn a check and no doc line.) | the two request transforms |
| "re-list the destination" / verify-on-disk sharp-edge lines *stay* (doctrine the proxy can't subsume) | — |

## Version-bump runbook (first-class deliverable, lives in the proxy repo)

Pin bump → re-capture canary baseline → diff tool list/schemas → re-run the repro driver (committed alongside) → re-verify the string-keyed behaviors (timeout strings, console-noise patterns) against upstream source → update baseline + strings in one commit. See `docs/bump-runbook.md`.

F22 keys on no string, so nothing there can go stale — but its **verdict** can, and it has **two independent halves**. The driver reports them separately: `F22-move-lies` (does upstream still report failure on a move that landed — GUID-verified, since a collision also leaves the destination present) and `F22b-bare-destination-relocates` (does a bare `destination` still resolve to `Assets/<name>` instead of beside the source). These are different mechanisms — verdict reporting versus argument resolution — so upstream can fix either alone. **Retire the refusal only when both read clean**; one clean half is not the criterion.

## Resolved by live repro (a scratch editor, bridge v10.1.0, server 10.1.0)

- Unpinned hard-error confirmed with mixed-version bridges visible (older bridges on the other editors register fine; handshake is version-tolerant).
- G22 and F22 reproduced as above; F23 not reproduced.
- `session_key→session_id`: nothing in the workspace reads either — non-issue.
- The repro driver ships in the proxy repo (parameterized); the bump runbook re-runs it.
